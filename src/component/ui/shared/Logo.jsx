import React from "react";
import { Link } from "react-router-dom";

const Logo = () => (
  <div className="logo">
    <Link to="/" className="logo__title">
      Cardiosers
    </Link>
  </div>
);

export default Logo;
